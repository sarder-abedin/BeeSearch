"""
mcp_servers/__init__.py
────────────────────────
Marker module for the `mcp_servers` package.

Holds no shared code or re-exports. `research_tools_server.py` is run
directly as a script (`python mcp_servers/research_tools_server.py`) rather
than imported through this package's namespace.
"""
