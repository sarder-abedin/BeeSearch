"""
ui/__init__.py
───────────────
Marker module for the `ui` package.

Holds no shared code or re-exports; submodules (theme, sidebar, landing,
helpers, glossary, and the `ui.tabs` subpackage) are imported directly by
callers such as `app.py` and `projects/mode{1,2}_*.py`.
"""
# ui package
